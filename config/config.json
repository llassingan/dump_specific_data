{
    "default_schema": "public",

    "custom_queries": [
        {
            "query": "SELECT account_code, account_name, balance from ibcore.account where account_type = 'X'",
            "output_table_name": "account",
            "output_schema": "ibcore",
            "batch_size": 1000
        }
    ]
}